package home

import (
    "yugiohdle/view/layout"
    "yugiohdle/view/component"
    "yugiohdle/model"
)

templ Show(card model.Card) {
    @layout.Base() {
        <h2>This is a random card: { card.Name }</h2>
        @component.Input("name")
    }
}
